# Bidder UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. UI-building tasks (5, 6) should ALSO load the `frontend-design` skill for the templates.

**Goal:** Веб-панель на VPS: владелец регулирует все настройки биддера (мин/макс ставка, шаг, коридор TACoS, доля бюджета, лимиты, `dry_run`, allowlist кампаний) из браузера и видит ленту решений/TACoS/ставки; воркер перечитывает конфиг на лету.

**Architecture:** FastAPI + Jinja2 (серверный рендер) за Caddy (HTTPS), отдельный systemd-сервис. UI и worker связаны ТОЛЬКО через `config/rules.yaml` (UI пишет, worker hot-reload читает) и `db/autopilot.db` (worker пишет, UI читает). Пакет `webui/`, переиспускает `core.*`/`connectors.*`.

**Tech Stack:** Python 3.12 (`.venv`), FastAPI, uvicorn, Jinja2, python-multipart, itsdangerous, SQLite. Тесты — plain-скрипты с `assert` (`.venv/bin/python test_X.py`, НЕ pytest); для webui — `fastapi.testclient.TestClient` внутри такого скрипта.

## Global Constraints

- Тесты — runnable-скрипты с `assert` + `print("✓ …")`, в `if __name__ == "__main__":`. Запуск `.venv/bin/python test_<mod>.py`. НЕ pytest. Полный набор должен оставаться зелёным.
- Русские докстринги/сообщения/подписи в UI. Сеть/браузер/планировщик — только в `worker.main()`; в UI сеть (кабинет) допустима, но только на чтение и с self-heal.
- UI и worker НЕ вызывают друг друга напрямую — только через `rules.yaml` и `db/autopilot.db`.
- Секреты (Kaspi логин/пароль/токен, UI пароль-хэш, секрет сессии) — ТОЛЬКО в `config/.env`, НИКОГДА в репозитории/UI/логах. `config/.env` в `.gitignore`.
- Все изменения аддитивны и обратно совместимы: новые поля/параметры опциональны с дефолтами; существующий деплой воркера продолжает работать.
- UI биндится на `127.0.0.1:8000`; наружу — только через Caddy (TLS). Ветка: `bidder-ui`. `dry_run` в yaml по умолчанию `true`.
- Валидация конфига ДО записи; запись `rules.yaml` атомарна (temp + `os.replace`).

## File Structure

- `core/settings_io.py` (new) — load/validate/save настроек поверх `rules.yaml`.
- `worker.py` (mod) — hot-reload cfg в `build_ctx`; `RulesConfig.campaign_ids`.
- `core/rules.py` (mod) — поле `campaign_ids` в `RulesConfig`.
- `webui/__init__.py`, `webui/app.py`, `webui/auth.py` (new) — FastAPI-приложение.
- `webui/templates/{base,login,settings,dashboard}.html`, `webui/static/app.css` (new).
- `core/store.py` (mod) — таблица `settings_audit` + методы.
- `requirements.txt` (mod), `config/.env.example` (mod).
- `deploy/kaspi-webui.service`, `deploy/Caddyfile` (new); `deploy/DEPLOY.md` (mod).
- Тесты: `test_settings_io.py`, `test_webui.py` (new); `test_worker.py`, `test_store.py` (mod).

---

### Task 1: `RulesConfig.campaign_ids` + `core/settings_io.py` (load/validate/save)

**Files:**
- Modify: `core/rules.py` (`RulesConfig` +поле `campaign_ids`)
- Create: `core/settings_io.py`
- Test: `test_settings_io.py` (new)

**Interfaces:**
- Produces:
  - `RulesConfig.campaign_ids: list[str] | None = None`.
  - `settings_io.SETTINGS_FIELDS: list[str]` — имена всех редактируемых полей.
  - `settings_io.load_settings(path: str) -> dict` — текущие значения из yaml (дефолты RulesConfig поверх).
  - `settings_io.validate_settings(data: dict) -> list[str]` — список ошибок (пусто = ок).
  - `settings_io.save_settings(path: str, data: dict) -> None` — валидирует и атомарно пишет yaml (кидает `ValueError` со списком, если невалидно).

- [ ] **Step 1: Написать падающий тест**

Создать `test_settings_io.py`:

```python
"""test_settings_io.py — валидация и запись настроек биддера (rules.yaml)."""
import os, tempfile

from core.rules import RulesConfig, load_rules_config
from core.settings_io import load_settings, validate_settings, save_settings, SETTINGS_FIELDS


def _tmp_yaml():
    d = tempfile.mkdtemp()
    p = os.path.join(d, "rules.yaml")
    # стартовый валидный конфиг из дефолтов
    save_settings(p, {f: getattr(RulesConfig(), f) for f in SETTINGS_FIELDS})
    return p


def test_validate_catches_bad_values():
    base = {f: getattr(RulesConfig(), f) for f in SETTINGS_FIELDS}
    assert validate_settings(base) == []                       # дефолты валидны
    bad = dict(base, bid_ceiling=0)                            # < min_bid
    assert any("bid_ceiling" in e for e in validate_settings(bad))
    bad = dict(base, sku_budget_fraction=1.5)                  # > 1
    assert any("sku_budget_fraction" in e for e in validate_settings(bad))
    bad = dict(base, target_tacos_low=0.2, target_tacos_high=0.1)  # low >= high
    assert any("tacos" in e.lower() for e in validate_settings(bad))
    bad = dict(base, max_bid_step=0)                           # < 1
    assert any("max_bid_step" in e for e in validate_settings(bad))
    print("✓ settings_io: validate ловит плохие значения")


def test_save_load_roundtrip_and_loadable_by_worker():
    p = _tmp_yaml()
    data = load_settings(p)
    data["min_bid"] = 3
    data["bid_ceiling"] = 40
    data["dry_run"] = False
    data["campaign_ids"] = ["2899523", "3032419"]
    save_settings(p, data)
    # читается обратно
    got = load_settings(p)
    assert got["min_bid"] == 3 and got["bid_ceiling"] == 40 and got["dry_run"] is False
    assert got["campaign_ids"] == ["2899523", "3032419"]
    # и воркерский загрузчик его понимает
    cfg = load_rules_config(p)
    assert cfg.min_bid == 3 and cfg.bid_ceiling == 40 and cfg.dry_run is False
    assert cfg.campaign_ids == ["2899523", "3032419"]
    print("✓ settings_io: save/load round-trip + load_rules_config совместим")


def test_save_rejects_invalid():
    p = _tmp_yaml()
    try:
        save_settings(p, dict(load_settings(p), bid_ceiling=0))
        assert False, "ожидали ValueError на невалидном конфиге"
    except ValueError:
        pass
    # файл не испорчен — по-прежнему загружается
    assert load_rules_config(p).bid_ceiling >= 1
    print("✓ settings_io: save отвергает невалидное, файл цел")


if __name__ == "__main__":
    test_validate_catches_bad_values()
    test_save_load_roundtrip_and_loadable_by_worker()
    test_save_rejects_invalid()
    print("-" * 60)
    print("✓ Все проверки settings_io прошли")
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv/bin/python test_settings_io.py`
Expected: FAIL — `ModuleNotFoundError: No module named 'core.settings_io'`.

- [ ] **Step 3: Реализация**

В `core/rules.py`, в `RulesConfig`, добавить поле в конец (после `dry_run`):

```python
    dry_run: bool = True
    campaign_ids: list[str] | None = None   # allowlist кампаний; None/пусто = все активные
```

Создать `core/settings_io.py`:

```python
"""settings_io.py — чтение/валидация/атомарная запись настроек биддера (rules.yaml).
UI пишет через эти функции; воркер читает тот же файл через load_rules_config."""
from __future__ import annotations

import os

from core.rules import RulesConfig, load_rules_config

# Порядок = порядок полей в форме UI.
SETTINGS_FIELDS = [
    "target_tacos_low", "target_tacos_high",
    "daily_sku_cost_limit", "sku_budget_fraction",
    "min_clicks_for_no_cart_cut", "cpc_spike_pct",
    "max_bid_step", "max_changes_per_day",
    "bid_ceiling", "min_bid", "min_score_for_raise",
    "dry_run", "campaign_ids",
]


def load_settings(path: str) -> dict:
    """Текущие значения (дефолты RulesConfig, перекрытые файлом)."""
    cfg = load_rules_config(path) if os.path.exists(path) else RulesConfig()
    return {f: getattr(cfg, f) for f in SETTINGS_FIELDS}


def validate_settings(data: dict) -> list[str]:
    """Список ошибок (пусто = валидно)."""
    errs: list[str] = []
    def num(name):
        try:
            return float(data.get(name))
        except (TypeError, ValueError):
            errs.append(f"{name}: не число")
            return None
    min_bid = num("min_bid"); ceil = num("bid_ceiling"); step = num("max_bid_step")
    low = num("target_tacos_low"); high = num("target_tacos_high")
    frac = num("sku_budget_fraction"); cap = num("daily_sku_cost_limit")
    changes = num("max_changes_per_day"); spike = num("cpc_spike_pct")
    clicks = num("min_clicks_for_no_cart_cut"); score = num("min_score_for_raise")
    if min_bid is not None and min_bid < 1:
        errs.append("min_bid: минимум 1")
    if ceil is not None and min_bid is not None and ceil < min_bid:
        errs.append("bid_ceiling: должен быть ≥ min_bid")
    if step is not None and step < 1:
        errs.append("max_bid_step: минимум 1")
    if low is not None and high is not None and not (0 < low < high):
        errs.append("target_tacos: должно быть 0 < low < high")
    if frac is not None and not (0 < frac <= 1):
        errs.append("sku_budget_fraction: должно быть в (0, 1]")
    if cap is not None and cap < 0:
        errs.append("daily_sku_cost_limit: не отрицательный")
    if changes is not None and changes < 0:
        errs.append("max_changes_per_day: не отрицательный")
    if spike is not None and spike < 0:
        errs.append("cpc_spike_pct: не отрицательный")
    if clicks is not None and clicks < 0:
        errs.append("min_clicks_for_no_cart_cut: не отрицательный")
    if score is not None and score < 0:
        errs.append("min_score_for_raise: не отрицательный")
    cids = data.get("campaign_ids")
    if cids not in (None, "") and not isinstance(cids, list):
        errs.append("campaign_ids: список строк или пусто")
    return errs


def save_settings(path: str, data: dict) -> None:
    """Валидирует и атомарно пишет rules.yaml. ValueError, если невалидно."""
    import yaml  # локальный импорт (как в load_rules_config)
    errs = validate_settings(data)
    if errs:
        raise ValueError("; ".join(errs))
    # нормализация типов
    out = {
        "target_tacos_low": float(data["target_tacos_low"]),
        "target_tacos_high": float(data["target_tacos_high"]),
        "daily_sku_cost_limit": float(data["daily_sku_cost_limit"]),
        "sku_budget_fraction": float(data["sku_budget_fraction"]),
        "min_clicks_for_no_cart_cut": int(float(data["min_clicks_for_no_cart_cut"])),
        "cpc_spike_pct": float(data["cpc_spike_pct"]),
        "max_bid_step": float(data["max_bid_step"]),
        "max_changes_per_day": int(float(data["max_changes_per_day"])),
        "bid_ceiling": float(data["bid_ceiling"]),
        "min_bid": float(data["min_bid"]),
        "min_score_for_raise": float(data["min_score_for_raise"]),
        "dry_run": bool(data["dry_run"]),
        "campaign_ids": list(data["campaign_ids"]) if data.get("campaign_ids") else None,
    }
    tmp = f"{path}.tmp"
    with open(tmp, "w") as f:
        yaml.safe_dump(out, f, allow_unicode=True, sort_keys=False)
    os.replace(tmp, path)
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv/bin/python test_settings_io.py`
Expected: PASS — все проверки.

- [ ] **Step 5: Регресс — rules-тесты не сломаны**

Run: `.venv/bin/python test_rules.py`
Expected: PASS (новое поле `campaign_ids` опционально; загрузчик его игнорирует, если нет в yaml).

- [ ] **Step 6: Коммит**

```bash
git add core/rules.py core/settings_io.py test_settings_io.py
git commit -m "feat(settings): settings_io (load/validate/save rules.yaml) + RulesConfig.campaign_ids"
```

---

### Task 2: hot-reload конфига в воркере

**Files:**
- Modify: `worker.py` (`build_ctx` перечитывает cfg; allowlist из `cfg.campaign_ids`; фолбэк на прошлый cfg при битом yaml)
- Test: `test_worker.py` (тест hot-reload через `build_ctx`-подобную логику)

**Interfaces:**
- Consumes: `load_rules_config` (существует), `RulesConfig.campaign_ids` (Task 1).
- Produces: воркер применяет актуальный `rules.yaml` на каждом цикле; `campaign_ids` берётся из yaml (фолбэк — env).

> Примечание: `build_ctx` определён внутри `main()` (`# pragma: no cover`), напрямую не тестируется. Чтобы hot-reload был тестируемым, ВЫНЕСТИ чтение cfg в чистую функцию `load_cfg_safe(path, prev)` на уровне модуля и покрыть её тестом; `build_ctx` вызывает её.

- [ ] **Step 1: Написать падающий тест**

В `test_worker.py` добавить (импорт `load_cfg_safe` сверху к существующему `from worker import ...`):

```python
def test_load_cfg_safe_hot_reload_and_fallback():
    import os, tempfile
    from worker import load_cfg_safe
    from core.settings_io import save_settings, load_settings
    p = os.path.join(tempfile.mkdtemp(), "rules.yaml")
    save_settings(p, dict(load_settings(p), dry_run=True, min_bid=1))
    cfg1 = load_cfg_safe(p, None)
    assert cfg1.dry_run is True
    # правим файл — следующий вызов видит новое значение (hot-reload)
    save_settings(p, dict(load_settings(p), dry_run=False, min_bid=5))
    cfg2 = load_cfg_safe(p, cfg1)
    assert cfg2.dry_run is False and cfg2.min_bid == 5
    # битый файл → возвращаем прошлый cfg, не падаем
    with open(p, "w") as f:
        f.write("%%% not yaml : : :")
    cfg3 = load_cfg_safe(p, cfg2)
    assert cfg3 is cfg2
    print("✓ worker: load_cfg_safe — hot-reload + фолбэк на прошлый cfg при битом yaml")
```

Добавить вызов в `__main__` (перед `print("-" * 60)`):

```python
    test_load_cfg_safe_hot_reload_and_fallback()
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv/bin/python test_worker.py`
Expected: FAIL — `ImportError: cannot import name 'load_cfg_safe' from 'worker'`.

- [ ] **Step 3: Реализация**

В `worker.py` на уровне модуля (после `run_cycle`/`_apply_and_log`, до `main`) добавить:

```python
def load_cfg_safe(path: str, prev):
    """Читает rules.yaml; при ошибке парсинга возвращает prev (не роняем воркер).
    prev=None и битый файл → пробрасываем ошибку (нечем фолбэкнуться на старте)."""
    from core.rules import load_rules_config
    try:
        return load_rules_config(path)
    except Exception as e:
        if prev is None:
            raise
        log.error("rules.yaml не читается (%s) — оставляю прошлый конфиг", e)
        return prev
```

В `main()`:
- завести путь и держать текущий cfg:
  ```python
      rules_path = os.environ.get("RULES_CONFIG", "config/rules.yaml")
      cfg_holder = {"cfg": load_cfg_safe(rules_path, None)}
  ```
  (заменяет прежнюю строку `cfg = load_rules_config(...)`; ниже, где читался
  `cfg` для merchant_id и т.п., использовать `cfg_holder["cfg"]`.)
- allowlist из cfg (фолбэк на env):
  ```python
      env_ids = os.environ.get("KASPI_CAMPAIGN_IDS", "").strip()
      env_ids = [c.strip() for c in env_ids.split(",") if c.strip()] or None
  ```
- `build_ctx` перечитывает cfg каждый цикл:
  ```python
      def build_ctx() -> WorkerContext:
          cfg = load_cfg_safe(rules_path, cfg_holder["cfg"])
          cfg_holder["cfg"] = cfg
          cookies = session.get_cookies()
          marketing = MarketingClient(
              merchant_id, cookies=cookies, dry_run=cfg.dry_run,
              on_auth_error=lambda: session.get_cookies(force_refresh=True))
          campaign_ids = cfg.campaign_ids or env_ids
          return WorkerContext(marketing=marketing, store=store, cfg=cfg,
                               campaign_ids=campaign_ids,
                               revenue_collector=revenue_collector)
  ```
  (лог запуска и расписания — без изменений.)

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv/bin/python test_worker.py`
Expected: PASS — все проверки worker, включая hot-reload.

- [ ] **Step 5: Коммит**

```bash
git add worker.py test_worker.py
git commit -m "feat(worker): hot-reload rules.yaml каждый цикл + allowlist из cfg.campaign_ids"
```

---

### Task 3: `settings_audit` в Store

**Files:**
- Modify: `core/store.py` (таблица `settings_audit` + `log_settings_change` + `get_settings_audit`)
- Test: `test_store.py`

**Interfaces:**
- Produces:
  - `Store.log_settings_change(user: str, field: str, old, new, ts: int)`.
  - `Store.get_settings_audit(limit: int = 50) -> list[dict]` (свежие сверху).

- [ ] **Step 1: Написать падающий тест**

В `test_store.py` добавить (после существующих):

```python
def test_settings_audit():
    st = new_store()
    st.log_settings_change("admin", "min_bid", 1, 3, ts=1000)
    st.log_settings_change("admin", "dry_run", True, False, ts=1001)
    rows = st.get_settings_audit()
    assert len(rows) == 2
    assert rows[0]["field"] == "dry_run"          # свежие сверху (по ts DESC)
    assert rows[0]["old"] == "True" and rows[0]["new"] == "False"
    assert rows[1]["field"] == "min_bid"
    print("✓ store: settings_audit пишет и читает правки конфига")
```

Добавить вызов в `__main__` (перед `print("-" * 60)`).

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv/bin/python test_store.py`
Expected: FAIL — `AttributeError: 'Store' object has no attribute 'log_settings_change'`.

- [ ] **Step 3: Реализация**

В `core/store.py`, в `_init_schema` (внутри `executescript`), добавить таблицу:

```sql
            CREATE TABLE IF NOT EXISTS settings_audit (
                ts INTEGER, user TEXT, field TEXT, old TEXT, new TEXT
            );
```

Добавить методы (рядом с другими):

```python
    def log_settings_change(self, user: str, field: str, old, new, ts: int):
        self._conn.execute(
            "INSERT INTO settings_audit (ts, user, field, old, new) VALUES (?,?,?,?,?)",
            (ts, user, field, str(old), str(new)),
        )
        self._conn.commit()

    def get_settings_audit(self, limit: int = 50) -> list[dict]:
        rows = self._conn.execute(
            "SELECT * FROM settings_audit ORDER BY ts DESC LIMIT ?", (limit,)
        ).fetchall()
        return [dict(r) for r in rows]
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv/bin/python test_store.py`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add core/store.py test_store.py
git commit -m "feat(store): аудит правок настроек (settings_audit)"
```

---

### Task 4: FastAPI-приложение + авторизация + деп-зависимости

**Files:**
- Create: `webui/__init__.py`, `webui/auth.py`, `webui/app.py`
- Modify: `requirements.txt`, `config/.env.example`
- Test: `test_webui.py` (new)

**Interfaces:**
- Consumes: `settings_io`, `Store`, `SessionMiddleware`.
- Produces:
  - `webui.auth.hash_password(pw) -> str`, `verify_password(pw, hashed) -> bool` (pbkdf2_hmac, stdlib).
  - `webui.app.create_app() -> FastAPI` — фабрика; читает `UI_USERNAME`, `UI_PASSWORD_HASH`, `UI_SECRET_KEY`, `RULES_CONFIG`, `DB_PATH` из env.
  - Роуты: `GET/POST /login`, `POST /logout`, `GET /` (заглушка дашборда пока), `GET/POST /settings`, `POST /dry-run`. Все, кроме `/login`+статики, требуют сессию (`request.session["user"]`).

- [ ] **Step 1: Установить зависимости в .venv**

Run: `.venv/bin/pip install fastapi "uvicorn[standard]" jinja2 python-multipart itsdangerous`
Expected: успех. Добавить их в `requirements.txt`:

```
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
```

Добавить в `config/.env.example`:

```
# --- Веб-панель (webui) ---
UI_USERNAME=admin
UI_PASSWORD_HASH=            # получить: .venv/bin/python -c "from webui.auth import hash_password; print(hash_password('ТВОЙ_ПАРОЛЬ'))"
UI_SECRET_KEY=              # случайная строка: .venv/bin/python -c "import secrets; print(secrets.token_hex(32))"
```

- [ ] **Step 2: Написать падающий тест**

Создать `test_webui.py`:

```python
"""test_webui.py — оффлайн-тест веб-панели через FastAPI TestClient."""
import os, tempfile
from fastapi.testclient import TestClient

from webui.auth import hash_password, verify_password
from webui.app import create_app
from core.rules import RulesConfig, load_rules_config
from core.settings_io import save_settings, load_settings, SETTINGS_FIELDS


def _client():
    d = tempfile.mkdtemp()
    rules = os.path.join(d, "rules.yaml")
    save_settings(rules, {f: getattr(RulesConfig(), f) for f in SETTINGS_FIELDS})
    db = os.path.join(d, "a.db")
    empty_env = os.path.join(d, "empty.env")
    open(empty_env, "w").close()   # чтобы create_app не подхватил реальный config/.env
    os.environ.update(
        UI_USERNAME="admin",
        UI_PASSWORD_HASH=hash_password("secret"),
        UI_SECRET_KEY="test-secret-key",
        RULES_CONFIG=rules, DB_PATH=db, ENV_FILE=empty_env,
    )
    app = create_app()
    return TestClient(app), rules


def test_password_hash_roundtrip():
    h = hash_password("secret")
    assert verify_password("secret", h) and not verify_password("wrong", h)
    print("✓ webui: pbkdf2 hash/verify")


def test_settings_requires_login():
    c, _ = _client()
    r = c.get("/settings", follow_redirects=False)
    assert r.status_code in (302, 303, 307) and "/login" in r.headers["location"]
    print("✓ webui: /settings без логина → редирект на /login")


def test_login_and_edit_settings():
    c, rules = _client()
    assert c.post("/login", data={"username": "admin", "password": "wrong"},
                  follow_redirects=False).status_code == 200  # остаёмся на форме
    r = c.post("/login", data={"username": "admin", "password": "secret"},
               follow_redirects=False)
    assert r.status_code in (302, 303)
    # правим настройки
    data = load_settings(rules)
    form = {k: ("on" if data[k] is True else "" if data[k] in (None, False) else data[k])
            for k in SETTINGS_FIELDS if k not in ("dry_run", "campaign_ids")}
    form.update(min_bid=3, bid_ceiling=40, campaign_ids="2899523,3032419")
    r = c.post("/settings", data=form, follow_redirects=False)
    assert r.status_code in (302, 303), r.text
    cfg = load_rules_config(rules)
    assert cfg.min_bid == 3 and cfg.bid_ceiling == 40
    assert cfg.campaign_ids == ["2899523", "3032419"]
    print("✓ webui: логин + сохранение настроек пишет rules.yaml")


def test_invalid_settings_rejected():
    c, rules = _client()
    c.post("/login", data={"username": "admin", "password": "secret"})
    data = load_settings(rules)
    form = {k: data[k] for k in SETTINGS_FIELDS if k not in ("dry_run", "campaign_ids")}
    form["bid_ceiling"] = 0     # < min_bid
    r = c.post("/settings", data=form)
    assert r.status_code == 200 and "bid_ceiling" in r.text   # показал ошибку
    assert load_rules_config(rules).bid_ceiling >= 1          # файл не изменён
    print("✓ webui: невалидные настройки отвергнуты, конфиг цел")


def test_dry_run_toggle():
    c, rules = _client()
    c.post("/login", data={"username": "admin", "password": "secret"})
    c.post("/dry-run", data={"dry_run": ""})       # выключить
    assert load_rules_config(rules).dry_run is False
    c.post("/dry-run", data={"dry_run": "on"})     # включить
    assert load_rules_config(rules).dry_run is True
    print("✓ webui: тумблер dry_run пишет конфиг")


if __name__ == "__main__":
    test_password_hash_roundtrip()
    test_settings_requires_login()
    test_login_and_edit_settings()
    test_invalid_settings_rejected()
    test_dry_run_toggle()
    print("-" * 60)
    print("✓ Все проверки webui прошли")
```

- [ ] **Step 3: Запустить — убедиться, что падает**

Run: `.venv/bin/python test_webui.py`
Expected: FAIL — `ModuleNotFoundError: No module named 'webui'`.

- [ ] **Step 4: Реализация — auth**

`webui/__init__.py`: пусто.

`webui/auth.py`:

```python
"""auth.py — хэш пароля (pbkdf2_hmac, stdlib) для веб-панели."""
from __future__ import annotations

import hashlib
import hmac
import os

_ITER = 200_000


def hash_password(pw: str) -> str:
    salt = os.urandom(16)
    dk = hashlib.pbkdf2_hmac("sha256", pw.encode(), salt, _ITER)
    return f"pbkdf2${_ITER}${salt.hex()}${dk.hex()}"


def verify_password(pw: str, hashed: str) -> bool:
    try:
        _, iters, salt_hex, dk_hex = hashed.split("$")
        dk = hashlib.pbkdf2_hmac("sha256", pw.encode(), bytes.fromhex(salt_hex), int(iters))
        return hmac.compare_digest(dk.hex(), dk_hex)
    except Exception:
        return False
```

- [ ] **Step 5: Реализация — app**

`webui/app.py`:

```python
"""app.py — FastAPI веб-панель управления биддером. Правит config/rules.yaml
(воркер перечитывает на лету), читает db/autopilot.db для дашборда."""
from __future__ import annotations

import os
import time

from fastapi import FastAPI, Request, Form
from fastapi.responses import RedirectResponse, HTMLResponse
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates
from starlette.middleware.sessions import SessionMiddleware

from core.settings_io import load_settings, save_settings, validate_settings, SETTINGS_FIELDS
from core.store import Store
from webui.auth import verify_password

_HERE = os.path.dirname(__file__)


def create_app() -> FastAPI:
    # Подхватить .env (для uvicorn через systemd), не перекрывая уже заданное окружение.
    try:
        from dotenv import load_dotenv
        load_dotenv(os.environ.get("ENV_FILE", "config/.env"), override=False)
    except ImportError:
        pass

    app = FastAPI()
    app.add_middleware(SessionMiddleware,
                       secret_key=os.environ.get("UI_SECRET_KEY", "dev-insecure"))
    app.mount("/static", StaticFiles(directory=os.path.join(_HERE, "static")), name="static")
    templates = Jinja2Templates(directory=os.path.join(_HERE, "templates"))

    rules_path = os.environ.get("RULES_CONFIG", "config/rules.yaml")
    db_path = os.environ.get("DB_PATH", "db/autopilot.db")
    username = os.environ.get("UI_USERNAME", "admin")
    pw_hash = os.environ.get("UI_PASSWORD_HASH", "")

    def user(request: Request):
        return request.session.get("user")

    @app.get("/login", response_class=HTMLResponse)
    def login_form(request: Request):
        return templates.TemplateResponse("login.html", {"request": request, "error": None})

    @app.post("/login")
    def login(request: Request, username_in: str = Form(alias="username"),
              password: str = Form(...)):
        if username_in == username and pw_hash and verify_password(password, pw_hash):
            request.session["user"] = username_in
            return RedirectResponse("/settings", status_code=303)
        return templates.TemplateResponse(
            "login.html", {"request": request, "error": "Неверный логин или пароль"},
            status_code=200)

    @app.post("/logout")
    def logout(request: Request):
        request.session.clear()
        return RedirectResponse("/login", status_code=303)

    @app.get("/", response_class=HTMLResponse)
    def dashboard(request: Request):
        if not user(request):
            return RedirectResponse("/login", status_code=303)
        # Фаза 2 наполнит; пока — заглушка со ссылкой на настройки.
        return templates.TemplateResponse("dashboard.html",
                                          {"request": request, "user": user(request)})

    @app.get("/settings", response_class=HTMLResponse)
    def settings_form(request: Request):
        if not user(request):
            return RedirectResponse("/login", status_code=303)
        return templates.TemplateResponse("settings.html", {
            "request": request, "user": user(request),
            "s": load_settings(rules_path), "fields": SETTINGS_FIELDS, "errors": []})

    @app.post("/settings")
    async def settings_save(request: Request):
        if not user(request):
            return RedirectResponse("/login", status_code=303)
        form = await request.form()
        cur = load_settings(rules_path)
        new = dict(cur)
        for f in SETTINGS_FIELDS:
            if f == "dry_run":
                new[f] = form.get("dry_run") == "on"
            elif f == "campaign_ids":
                raw = (form.get("campaign_ids") or "").strip()
                new[f] = [x.strip() for x in raw.split(",") if x.strip()] or None
            else:
                new[f] = form.get(f)
        errors = validate_settings(new)
        if errors:
            return templates.TemplateResponse("settings.html", {
                "request": request, "user": user(request),
                "s": new, "fields": SETTINGS_FIELDS, "errors": errors}, status_code=200)
        store = Store(db_path)
        ts = int(time.time())
        for f in SETTINGS_FIELDS:
            if str(cur.get(f)) != str(new.get(f)):
                store.log_settings_change(user(request), f, cur.get(f), new.get(f), ts)
        save_settings(rules_path, new)
        return RedirectResponse("/settings", status_code=303)

    @app.post("/dry-run")
    async def dry_run_toggle(request: Request):
        if not user(request):
            return RedirectResponse("/login", status_code=303)
        form = await request.form()
        cur = load_settings(rules_path)
        cur["dry_run"] = form.get("dry_run") == "on"
        save_settings(rules_path, cur)
        Store(db_path).log_settings_change(user(request), "dry_run",
                                           not cur["dry_run"], cur["dry_run"], int(time.time()))
        return RedirectResponse("/settings", status_code=303)

    return app
```

Логика `POST /settings`: `dry_run` — чекбокс (`on`); `campaign_ids` — строка через
запятую → список или `None`; остальные поля — как строки из формы (типы приведёт
`save_settings`). При ошибках валидации — рендер `settings.html` с `errors` и
введёнными значениями (status 200), файл НЕ трогаем; при успехе — записать
изменённые поля в аудит и `save_settings`, редирект 303. Точное поведение
зафиксировано в `test_webui.py`.

- [ ] **Step 6: Реализация — шаблоны (загрузить skill frontend-design)**

Создать `webui/static/app.css` (тёмная тема, аккуратная типографика, адаптив) и
шаблоны Jinja2. **Загрузи skill `frontend-design`** и оформи по нему; функциональные
требования:
- `base.html` — общий каркас: шапка с названием, ссылки «Настройки»/«Дашборд»,
  кнопка «Выйти» (POST /logout), слот контента, подключение `/static/app.css`.
- `login.html` — форма (username, password), блок ошибки `{{ error }}`.
- `settings.html` — форма POST `/settings`: поле на каждый элемент `fields`
  (числовые `input type=number step=any`, `dry_run` — переключатель, `campaign_ids`
  — текст «через запятую»), подписи по-русски с подсказками (что делает каждая
  ручка), блок `errors` сверху, кнопка «Сохранить». Отдельная форма POST `/dry-run`
  с подтверждением при выключении. Внизу — таблица последних правок
  (`settings_audit`).
- `dashboard.html` — пока заглушка (Фаза 2): приветствие + ссылка на настройки.

Держи HTML семантичным и адаптивным; значения полей заполняй из `s[field]`.

- [ ] **Step 7: Запустить — убедиться, что проходит**

Run: `.venv/bin/python test_webui.py`
Expected: PASS — все проверки webui.

- [ ] **Step 8: Полный набор тестов**

Run:
```bash
for t in test_revenue test_marketing test_session test_reconcile test_rules test_store test_worker test_analyst test_settings_io test_webui; do .venv/bin/python $t.py >/dev/null && echo "OK $t" || echo "FAIL $t"; done
```
Expected: OK по всем 10.

- [ ] **Step 9: Коммит**

```bash
git add webui requirements.txt config/.env.example test_webui.py
git commit -m "feat(webui): FastAPI-панель — логин + редактор настроек + dry_run + аудит"
```

---

### Task 5: Дашборд (данные из БД + бюджеты кабинета)

**Files:**
- Modify: `webui/app.py` (наполнить `GET /`), `webui/templates/dashboard.html`, при необходимости `core/store.py` (хелпер последнего снапшота)
- Test: `test_webui.py`

**Interfaces:**
- Consumes: `Store.get_decisions_for_day`, `Store.get_tacos_daily`, снапшоты; `MarketingClient.list_active_campaigns` (бюджеты, best-effort).
- Produces: `GET /` рендерит дашборд с решениями за день, TACoS, текущими ставками; бюджеты кампаний — если сессия кабинета доступна, иначе блок скрыт (не роняем страницу).

- [ ] **Step 1: Написать падающий тест**

В `test_webui.py` добавить (после `test_dry_run_toggle`) тест, что дашборд под логином отдаёт данные из БД:

```python
def test_dashboard_shows_decisions_from_db():
    c, rules = _client()
    # наполнить БД решением
    from core.store import Store
    from core.rules import Decision
    import time as _t
    db = os.environ["DB_PATH"]
    st = Store(db)
    day = __import__("datetime").date.today().isoformat()
    st.log_decision(Decision("SKU1", "M1", 10, 8, "lower", "slow", "TACoS высокий"),
                    ts=int(_t.time()), day=day, applied=False, campaign_id="2899523")
    c.post("/login", data={"username": "admin", "password": "secret"})
    r = c.get("/")
    assert r.status_code == 200
    assert "SKU1" in r.text and "TACoS высокий" in r.text
    print("✓ webui: дашборд показывает решения из БД")
```

Добавить вызов в `__main__`.

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv/bin/python test_webui.py`
Expected: FAIL — дашборд-заглушка не содержит `SKU1` (`assert ... "SKU1" in r.text`).

- [ ] **Step 3: Реализация**

В `webui/app.py`, заменить тело `GET /` так, чтобы собрать контекст из БД:
- `day = datetime.now(ZoneInfo("Asia/Almaty")).date().isoformat()`;
- `store = Store(db_path)`; `decisions = store.get_decisions_for_day(day)`;
  `tacos = store.get_tacos_daily(day)`;
- текущие ставки: по каждому `sku` из decisions/tacos взять последний снапшот
  (использовать `store.get_prev_avg_cpc` есть, но для bid добавить хелпер
  `store.get_latest_snapshot(sku) -> dict | None` — SELECT * ... ORDER BY ts DESC
  LIMIT 1; при необходимости добавить в `core/store.py` + мини-тест в `test_store.py`);
- бюджеты кампаний — best-effort: если задан путь `STORAGE_STATE` и файл есть,
  через `SessionManager`+`MarketingClient.list_active_campaigns` (self-heal); любую
  ошибку глушим и показываем без бюджетов. Кэш в модульной переменной на ~60с.
- Рендерить `dashboard.html` с `{decisions, tacos, budgets, day, user}`.

`dashboard.html` — таблицы: решения (день, кампания, sku, действие, причина,
применено), TACoS по SKU, бюджеты кампаний (если есть). По frontend-design.

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv/bin/python test_webui.py`
Expected: PASS.

- [ ] **Step 5: Полный набор**

Run: тот же цикл из 10 тестов. Expected: OK.

- [ ] **Step 6: Коммит**

```bash
git add webui core/store.py test_webui.py test_store.py
git commit -m "feat(webui): дашборд — решения/TACoS/ставки из БД + бюджеты кампаний"
```

---

### Task 6: Деплой webui (systemd + Caddy + DEPLOY.md)

**Files:**
- Create: `deploy/kaspi-webui.service`, `deploy/Caddyfile`
- Modify: `deploy/DEPLOY.md`

**Interfaces:** артефакты деплоя; функциональной логики нет (проверка — запуск приложения без ошибок импорта).

- [ ] **Step 1: systemd-юнит для uvicorn**

Создать `deploy/kaspi-webui.service`:

```ini
# Веб-панель управления биддером (uvicorn за Caddy).
#   sudo cp deploy/kaspi-webui.service /etc/systemd/system/
#   sudo systemctl daemon-reload && sudo systemctl enable --now kaspi-webui
[Unit]
Description=Kaspi Bidder Web UI
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=kaspi
WorkingDirectory=/opt/kaspi-ads-autopilot
Environment=ENV_FILE=/opt/kaspi-ads-autopilot/config/.env
Environment=PYTHONUNBUFFERED=1
ExecStart=/opt/kaspi-ads-autopilot/.venv/bin/uvicorn webui.app:create_app --factory --host 127.0.0.1 --port 8000
Restart=on-failure
RestartSec=15

[Install]
WantedBy=multi-user.target
```

> Реализатору: `webui.app` должен подхватывать `.env` при импорте (как `worker.main`)
> — добавь в `webui/app.py` вверху `create_app` загрузку dotenv из `ENV_FILE`
> (try/except ImportError), чтобы uvicorn через systemd видел переменные.

- [ ] **Step 2: Caddyfile (авто-HTTPS)**

Создать `deploy/Caddyfile`:

```
# Замени <домен> на домен/hostname, указывающий на VPS (напр. cloud-001.h-161398.kz).
# Если публичного домена нет — используй ": 443 { tls internal ... }" (самоподпись).
<домен> {
    reverse_proxy 127.0.0.1:8000
}
```

- [ ] **Step 3: Дополнить `deploy/DEPLOY.md`**

Добавить раздел «Веб-панель (UI)»: установить Caddy (`apt install caddy`),
положить `Caddyfile` в `/etc/caddy/Caddyfile` (прописать домен), сгенерировать
`UI_PASSWORD_HASH`/`UI_SECRET_KEY` и внести в `config/.env`, поставить
`kaspi-webui.service`, `ufw allow 80,443`, проверить `https://<домен>/login`.
Отметить: UI слушает только 127.0.0.1; наружу — Caddy(TLS); прямой 8000 закрыт.

- [ ] **Step 4: Проверка импорта/старта приложения**

Run: `.venv/bin/python -c "from webui.app import create_app; create_app(); print('APP_OK')"`
Expected: `APP_OK` (приложение собирается без ошибок).

- [ ] **Step 5: Полный набор тестов**

Run: цикл из 10 тестов. Expected: OK по всем.

- [ ] **Step 6: Коммит**

```bash
git add deploy/kaspi-webui.service deploy/Caddyfile deploy/DEPLOY.md
git commit -m "feat(deploy): webui — systemd uvicorn + Caddy(HTTPS) + runbook"
```

---

## Post-plan: выкат UI на VPS (после реализации)

Не часть коммитов:
- `git pull` на VPS; `.venv/bin/pip install -r requirements.txt`.
- Сгенерировать `UI_PASSWORD_HASH`/`UI_SECRET_KEY`, внести в `config/.env`.
- `apt install caddy`, прописать домен в `Caddyfile`, поставить оба сервиса.
- Открыть `https://<домен>/login`, войти, покрутить настройки, убедиться, что воркер
  подхватил (в `journalctl -u kaspi-autopilot` видно новое поведение со след. тика).
